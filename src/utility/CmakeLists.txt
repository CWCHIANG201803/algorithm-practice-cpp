add_library(utility "")

target_sources(
    utility
    PRIVATE
    inputProcessUtility.cpp
    PUBLIC
        ${CMAKE_CURRENT_LIST_DIR}/inputProcessUtility.hpp
    )

message(STATUS "the cmake current list dir is ${CMAKE_CURRENT_LIST_DIR}")

target_include_directories(
    utility
    PUBLIC
        ${CMAKE_CURRENT_LIST_DIR}
    )
